<center><FONT  FACE="courier" COLOR=blue  BGColor=yellow ><h1> Listing Students</h1></font>
<table bgcolor="White	"border=2>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th>Address</th>
    <th>Mobile</th>
    <th>Email</th>
    <th>Am paid</th>
    <th>Out of</th>
    <th>Check</th>
    <th>Created at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @students.each do |student| %>
  <tr>
    <td><%= student.first_name %></td>
    <td><%= student.last_name %></td>
    <td><%= student.address %></td>
    <td><%= student.mobile %></td>
    <td><%= student.email %></td>
    <td><%= student.am_paid %></td>
    <td><%= student.out_of %></td>
    <td><%= student.check %></td>
    <td><%= student.created_at %></td>
    <td><%= link_to 'Show', student %></td>
    <td><%= link_to 'Edit', edit_student_path(student) %></td>
    <td><%= link_to 'Destroy', student, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
 <body background-image: url('images/st.jpg');></body>

<br />

<%= link_to 'New Student', new_student_path %>
